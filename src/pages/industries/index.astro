---
import Layout from "../../layouts/Layout.astro";
import Breadcrumbs from "../../components/Breadcrumbs.astro";
import CTABand from "../../components/CTABand.astro";
import TrustBar from "../../components/TrustBar.astro";
import { industries } from "../../data/industries";
---

<Layout
  title="Industries We Serve | KCS Krystal Clear Service"
  description="Commercial cleaning for offices, private schools, medical facilities, gyms, churches, HOAs, property managers & retail across the Bay Area & Sacramento."
>
  <Breadcrumbs crumbs={[{ label: "Industries" }]} />

  <section class="mx-auto max-w-7xl px-4 py-10 sm:px-6">
    <div class="max-w-3xl">
      <h1 class="text-4xl font-extrabold tracking-tight text-navy-900">Industries We Serve</h1>
      <p class="mt-4 text-lg leading-relaxed text-slate-600">
        Every facility type has its own standards, schedules, and headaches. Our crews are trained for the specific
        demands of each — from private school campuses that need background-checked night crews to medical suites with
        strict disinfection protocols.
      </p>
    </div>

    <div class="mt-12 grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
      {
        industries.map((i) => (
          <a
            href={`/industries/${i.slug}/`}
            class="group rounded-2xl border border-slate-100 bg-white p-6 shadow-sm transition hover:-translate-y-0.5 hover:border-crystal-200 hover:shadow-lg"
          >
            <h2 class="text-lg font-bold text-navy-900 group-hover:text-crystal-700">{i.name}</h2>
            <p class="mt-2 text-sm leading-relaxed text-slate-600">{i.blurb}</p>
            <span class="mt-4 inline-block text-sm font-bold text-crystal-600">Learn more →</span>
          </a>
        ))
      }
    </div>
  </section>

  <TrustBar />
  <CTABand />
</Layout>
